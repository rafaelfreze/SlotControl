import type { ReportFilters } from "./source-contract.ts";
import { REPORT_TIMEZONE, REPORT_VERSION, reportDates } from "./filters.ts";
import { buildCsv, buildZip, sanitizeExportValue, sha256, stableJson, type ExportColumn, type ExportRow, type PackageFile } from "./export-format.ts";

export type AuditDatasets = Record<string, ExportRow[]>;
export type PackagedAudit = { datasets: AuditDatasets; warnings: string[]; incompleteSources: string[] };
const columnList = (value: string): ExportColumn[] => value.split(" ").map((key) => ({ key, label: key }));
const summaryHeaders: Record<string, string> = {
  period_start: "Início do período (UTC)", period_end: "Fim exclusivo do período (UTC)", environment: "Ambiente", asset: "Ativo", symbol: "Par", mode: "Modo", capital_start: "Capital inicial", capital_end: "Capital final", free_capital: "Capital livre", committed_capital: "Capital comprometido", realized_pnl: "Lucro realizado", open_pnl: "Lucro/prejuízo em aberto", total_result: "Resultado total", gains: "Gains realizados", operations: "Operações", cycles: "Ciclos", slots: "Slots físicos", buys: "Compras", tps: "Take profits", orders_open: "Ordens abertas", orders_filled: "Ordens concluídas", orders_cancelled: "Ordens canceladas", fills: "Preenchimentos", missed_levels: "Níveis perdidos", errors: "Erros registrados", last_price: "Último preço observado", last_event: "Último evento (UTC)", last_reconciliation: "Última reconciliação (UTC)", last_execution: "Última execução (UTC)", health: "Saúde do motor", currency: "Moeda", evidence_basis: "Base da evidência", warnings: "Ressalvas"
};
Object.assign(summaryHeaders, { take_profits: "Take profits registrados", observed_until: "Evidência coletada até (UTC)", capital_basis: "Base do cálculo de capital", open_operations: "Operações em aberto", completed_cycles: "Ciclos concluídos", fill_count_basis: "Base da contagem de fills", health_basis: "Base da avaliação de saúde", snapshot_at: "Snapshot coletado em (UTC)", quote_asset: "Moeda de cotação", snapshot_exceeds_period: "Snapshot posterior ao período", tps: "Take profits" });
delete summaryHeaders.tps;
export const REPORT_FILES = [
  { key: "summary", name: "00_RESUMO.csv", description: "Resumo para leitura humana", columns: Object.entries(summaryHeaders).map(([key, label]) => ({ key, label })) },
  { key: "cycles", name: "01_CICLOS.csv", description: "Ciclos e motivos de reset", columns: columnList("environment asset symbol cycle_id status completion_reason anchor_price gain_rate entry_spacing capital_start capital_end slot_count started_at completed_at duration_ms operations gains realized_pnl missed_levels errors reset_reason next_cycle_id") },
  { key: "slots", name: "02_SLOTS.csv", description: "Identidade física e contexto dos slots", columns: columnList("environment asset symbol cycle_id physical_slot_number logical_level operation_sequence entry_state status gain_count_test gain_count_cycle initial_balance current_balance accumulated_profit allocation buy_price current_price_snapshot take_profit_price buy_status tp_status quantity notional open_pnl realized_pnl opened_at closed_at recycled_at armed_at missed_at evidence_basis") },
  { key: "operations", name: "03_OPERACOES.csv", description: "Operações e resultado líquido", columns: columnList("environment operation_id cycle_id slot logical_level sequence symbol side entry_price quantity allocation take_profit_price gain_target_percent opened_at closed_at gross_profit fees net_profit result trigger_source buy_client_order_id sell_client_order_id") },
  { key: "orders", name: "04_ORDENS.csv", description: "Ordens e ownership por ambiente", columns: columnList("environment exchange symbol order_id client_order_id ownership_verified cycle_id slot operation_id side order_type status requested_price average_fill_price requested_quantity executed_quantity remaining_quantity created_at first_fill_at filled_at canceled_at replaced_by reason reconciliation_status evidence_basis") },
  { key: "events", name: "05_EVENTOS.csv", description: "Timeline com nomes históricos preservados", columns: columnList("timestamp_utc timestamp_local environment asset symbol cycle_id slot operation_id event_type previous_state next_state market_price trigger_price previous_entry_price reentry_price balance_before balance_after gain_count_before gain_count_after other_open_positions local_recycle_vs_global_reset reset_after_last_tp old_next_buy_canceled new_cycle_started initial_reentry_filled new_tp_created next_buy_armed reset_latency_ms recovery_source details idempotency_key source severity") },
  { key: "gains", name: "06_GAINS.csv", description: "Gains e acumulados por slot e ativo", columns: columnList("environment asset symbol cycle_id physical_slot operation_sequence entry_price tp_price target_percent gross_gain fees net_gain balance_before balance_after opened_at gain_at duration_ms cumulative_slot_gains cumulative_asset_gains") },
  { key: "capital", name: "07_CAPITAL_COMPOUNDING.csv", description: "Ledger de capital e reinvestimento", columns: columnList("timestamp environment asset slot event balance_before realized_profit fees contribution withdrawal balance_after next_allocation source_operation reason") },
  { key: "market", name: "08_MERCADO_GATILHOS.csv", description: "Mercado, cruzamentos e ações esperadas", columns: columnList("candle_open_at environment asset symbol open high low close active_cycle active_slot next_buy_trigger tp_trigger buy_expected buy_observed tp_expected tp_observed trigger_type trigger_price first_cross_at expected_action observed_action latency_ms trigger_result missed_level ambiguity notes") },
  { key: "reconciliation", name: "09_RECONCILIACAO.csv", description: "Reconciliações e divergências", columns: columnList("timestamp environment run_id symbol local_state exchange_state matches divergences recovered_actions unresolved duration_ms result error") },
  { key: "alerts", name: "10_ALERTAS_ERROS.csv", description: "Erros, ambiguidades e evidências ausentes", columns: columnList("timestamp environment severity asset cycle_id slot code message expected_behavior observed_behavior resolved resolved_at resolution related_event related_order related_operation") },
  { key: "rules", name: "11_REGRAS_CONFIGURACAO.csv", description: "Regras efetivas e proveniência", columns: columnList("effective_from effective_until environment asset symbol cycle_id parameter value unit snapshot evidence_scope source version notes") },
  { key: "checks", name: "12_CHECKS_AUDITORIA.csv", description: "PASS, WARNING e FAIL explicados", columns: columnList("code status explanation environment asset cycle_id slot expected observed evidence") },
  { key: "testnet", name: "13_TESTNET.csv", description: "BINANCE TESTNET / FUNDOS FICTÍCIOS", columns: columnList("environment asset symbol run_id status capital_start capital_end realized_pnl gains orders_open orders_filled orders_cancelled missed_levels last_reconciliation error evidence_basis") },
  { key: "real", name: "14_REAL.csv", description: "Production somente leitura e preparação LIVE", columns: columnList("environment asset symbol mode live_blocked coinops_real_orders coinops_real_trades connection_status last_reconciliation hard_caps filters checklist evidence_basis") }
  , { key: "decisions", name: "15_ESTRATEGIA_DECISOES.csv", description: "Decisão única, versão, despacho, ACK e latência", columns: columnList("environment asset strategy_version decision_id cycle_id slot_id operation_id operation_sequence action_type target_price target_notional priority reason expected_next_state observed_next_state decision_created_at decision_dispatched_at exchange_ack_at completed_at result error latency_ms first_cross_at order_resident_at root_cause resolved_by_version evidence_basis") }
] as const;

export function columnsFor(definition: typeof REPORT_FILES[number], rows: ExportRow[]) {
  const known = new Set(definition.columns.map((column) => column.key));
  // New contract fields are preserved, never silently discarded by a fixed CSV schema.
  const extra = [...new Set(rows.flatMap((row) => Object.keys(row)))].filter((key) => !known.has(key)).sort();
  return [...definition.columns, ...extra.map((key) => ({ key, label: summaryHeaders[key] || key }))];
}

function text(value: unknown) { return value == null ? "não disponível" : String(value); }
function number(value: unknown) { return typeof value === "number" && Number.isFinite(value) ? value.toLocaleString("pt-BR", { maximumFractionDigits: 8 }) : text(value); }
export function buildReportSummary(report: PackagedAudit, filters: ReportFilters, generatedAt: string) {
  const dates = reportDates(filters), checks = report.datasets.checks || [];
  const statuses = ["PASS", "WARNING", "FAIL"].map((status) => `${checks.filter((row) => row.status === status).length} ${status}`).join(" · ");
  const summaries = (report.datasets.summary || []).map((row) => `- **${text(row.asset)} ${text(row.environment)} (${text(row.symbol)})**: ${text(row.gains)} gains, ${text(row.operations)} operações e ${text(row.cycles)} ciclos no período. Lucro realizado: ${number(row.realized_pnl)} ${text(row.currency || row.quote_asset || (String(row.symbol).endsWith("BRL") ? "BRL" : "USDC"))}. Capital: ${number(row.capital_start)} → ${number(row.capital_end)}. P&L aberto: ${number(row.open_pnl)}. Compras: ${text(row.buys)}; TPs: ${text(row.take_profits ?? row.tps)}. Níveis perdidos registrados: ${text(row.missed_levels)}; erros: ${text(row.errors)}. Saúde: ${text(row.health)}; última execução: ${text(row.last_execution)}.`);
  const resets = (report.datasets.cycles || []).filter((row) => row.reset_reason || row.next_cycle_id).length;
  return ["# CoinOps — relatório auditável", "", `Período: **${dates.start} a ${dates.end}**, horário ${REPORT_TIMEZONE}.`, `Gerado em ${generatedAt} (UTC). Versão ${REPORT_VERSION}.`, "", "## O que foi analisado", "", "Dados persistidos do CoinOps, separados por ambiente e ativo. A exportação não consulta nem envia ordens à Binance. Testnet usa fundos fictícios; Shadow simula sobre mercado real. Production permanece somente leitura e LIVE bloqueado.", "", ...summaries, "", `Resets com vínculo/motivo no pacote: ${resets}. Checks: **${statuses}**.`, "", "## Como interpretar", "", "PASS significa que o teste descrito encontrou evidência suficiente dentro do escopo indicado. WARNING indica limite de evidência, ambiguidade ou situação que exige revisão. FAIL indica inconsistência comprovada nos dados observados. Um pacote com fontes incompletas não certifica que todas as regras foram obedecidas.", "", "Campos vazios significam não disponível/não aplicável; não equivalem a zero. Estado atual não é automaticamente estado histórico. Consulte evidence_basis e as ressalvas para distinguir snapshots de fatos imutáveis. Ordens/trades manuais observados pela reconciliação não são operações CoinOps.", "", "## Ressalvas e fontes incompletas", "", ...([...new Set([...report.warnings, ...report.incompleteSources])].map((warning) => `- ${warning}`).length ? [...new Set([...report.warnings, ...report.incompleteSources])].map((warning) => `- ${warning}`) : ["Nenhuma fonte incompleta identificada no carregamento; consulte os checks para o alcance da validação."]), "", "## Arquivos e relações", "", ...REPORT_FILES.map((file) => `- ${file.name}: ${file.description} (${(report.datasets[file.key] || []).length} linhas).`), "- AUDITORIA_COMPLETA.json: dados normalizados, relações e checks para análise técnica/IA.", "- manifest.json: versão, período, proveniência, contagens, hashes SHA-256 e limitações.", "", "Use environment + cycle_id/run_id + physical_slot_number para localizar um slot no contexto correto; operation_sequence diferencia sua reutilização. operation_id/source_operation liga operação, gain e crédito. client_order_id comprova o vínculo técnico da ordem. IDs pertencem a ambientes separados.", "", "## Formato e privacidade", "", "CSVs: UTF-8 com BOM, separador ponto e vírgula (;), CRLF, cabeçalhos entre aspas e números com ponto decimal para interoperabilidade. No Excel brasileiro, importe em Dados → De Texto/CSV, UTF-8, delimitador ; e localidade Inglês (Estados Unidos) para as colunas numéricas. Datas ISO; persistência UTC e horário local explícito quando relevante. Texto que poderia ser fórmula recebe apóstrofo de proteção.", "", "O pacote padrão inclui somente a evidência de mercado selecionada ao redor dos gatilhos/eventos; candles 1m completos têm download separado. Ausência de candle/cobertura ou ordem intrabar indeterminada é registrada como limitação/AMBIGUOUS, nunca como execução inventada. Candles Production não comprovam execução no livro Testnet.", "", "Snapshots são lidos por páginas; não representam uma transação única do banco. Eventos e créditos imutáveis conservam sua identidade. Secrets, tokens, cookies e credenciais são excluídos. O relatório contém informação financeira privada: compartilhe somente com quem deve auditá-la.", "", "## Solicitação sugerida para auditoria", "", "> Audite completamente o funcionamento do robô neste período. Cruze regras efetivas, mercado, eventos, ciclos, slots, operações, ordens, ledger e checks. Diga quais regras têm evidência suficiente de cumprimento, quais foram violadas e quais não podem ser verificadas. Não trate campos vazios ou fontes incompletas como zero/PASS.", ""].join("\n");
}

export function buildReportPackage(reportInput: PackagedAudit, filters: ReportFilters, generatedAt: string, commitSha: string | null = null) {
  const report = sanitizeExportValue(reportInput) as PackagedAudit;
  const rowCounts = Object.fromEntries(REPORT_FILES.map((definition) => [definition.name, (report.datasets[definition.key] || []).length]));
  const files: PackageFile[] = REPORT_FILES.map((definition) => ({ name: definition.name, content: buildCsv(report.datasets[definition.key] || [], columnsFor(definition, report.datasets[definition.key] || [])) }));
  files.push({ name: "AUDITORIA_COMPLETA.json", content: stableJson({ report_version: REPORT_VERSION, generated_at: generatedAt, timezone: REPORT_TIMEZONE, period_start: filters.start, period_end: filters.end, environments: filters.environments, assets: filters.assets, app_commit_sha: commitSha, relationships: { slot: ["environment", "cycle_id", "physical_slot_number"], operation: ["environment", "operation_id"], ownership: ["environment", "client_order_id"], ledger: ["environment", "source_operation"], note: "IDs e timestamps preservados; campos indisponíveis são null. IDs de reconciliação não são IDs de ciclo." }, datasets: report.datasets, warnings: report.warnings, incomplete_sources: report.incompleteSources }) });
  files.push({ name: "RESUMO.md", content: buildReportSummary(report, filters, generatedAt) });
  const manifest = { report_version: REPORT_VERSION, generated_at: generatedAt, timezone: REPORT_TIMEZONE, period_start: filters.start, period_end: filters.end, period_end_exclusive: true, environments: filters.environments, assets: filters.assets, schema_version: "coinops/robot-v1/reports-v2", app_commit_sha: commitSha, included_files: [...REPORT_FILES.map((file) => file.name), "AUDITORIA_COMPLETA.json", "manifest.json", "RESUMO.md"], row_counts: rowCounts, warnings: report.warnings, incomplete_sources: report.incompleteSources, file_metadata: Object.fromEntries(files.map((file) => [file.name, { bytes: Buffer.byteLength(file.content), sha256: sha256(file.content) }])), integrity_note: "Hashes cobrem todos os arquivos de conteúdo; manifest.json não contém hash de si próprio.", csv: { encoding: "UTF-8 BOM", delimiter: ";", newline: "CRLF", decimal_separator: ".", formula_protection: true }, source_scope: "authenticated user + server-resolved tenant + product + RLS", financial_writes_from_export: 0 };
  files.push({ name: "manifest.json", content: stableJson(manifest) });
  const dates = reportDates(filters);
  return { files, manifest, filename: `coinops-report-${dates.start}_${dates.end}.zip`, zip: () => buildZip(files) };
}
